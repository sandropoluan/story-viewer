# Path to cookies file or inline JSON (export from Cookie-Editor on instagram.com)
IG_BROWSER_COOKIES=./cookies.json

# Optional: reuse saved session (populated after successful cookie login)
# IG_SESSION_FILE=ig-session.json
# IG_SESSION=<base64 or JSON>

# Debug: log API request/response (DEBUG=1)
# DEBUG=1
